<resources xmlns:tools="http://schemas.android.com/tools">
  <!-- Base application theme. -->
  <style name="Theme.GahoelChat" parent="Theme.MaterialComponents.DayNight.NoActionBar">
    <item name="android:fontFamily">@font/poppins_semibold</item>
    <item name="fontFamily">@font/poppins_semibold</item> <!-- target android sdk versions < 26 and > 14 if theme other than AppCompat -->
    <!-- Primary brand color. -->
    <item name="colorPrimary">@color/blueGrey</item>
    <item name="colorPrimaryVariant">@color/blueGrey</item>
    <item name="colorOnPrimary">@color/white</item>
    <!-- Secondary brand color. -->
    <item name="colorSecondary">@color/lightBlueGrey</item>
    <item name="colorSecondaryVariant">@color/lightBlueGrey</item>
    <item name="colorOnSecondary">@color/white</item>
    <!-- Status bar color. -->
    <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
    <!-- Customize your theme here. -->
  </style>
</resources>